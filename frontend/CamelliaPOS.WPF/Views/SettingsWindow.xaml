<Window x:Class="CamelliaPOS.WPF.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Settings" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="System Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBox materialDesign:HintAssist.Hint="Tax Percentage"
                     Text="{Binding Settings.TaxPercentage, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" />

            <TextBox materialDesign:HintAssist.Hint="Currency"
                     Text="{Binding Settings.Currency, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" />

            <TextBox materialDesign:HintAssist.Hint="Session Timeout (minutes)"
                     Text="{Binding Settings.SessionTimeoutMinutes, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,20" />

            <TextBlock Text="Change Password" FontWeight="Bold" Margin="0,10,0,5"/>

            <PasswordBox x:Name="CurrentPasswordBox"
                         materialDesign:HintAssist.Hint="Current Password"
                         Margin="0,0,0,10"/>

            <PasswordBox x:Name="NewPasswordBox"
                         materialDesign:HintAssist.Hint="New Password"
                         Margin="0,0,0,10"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Reset System"
                    Command="{Binding ResetSystemCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,10,0"/>
            <Button Content="Change Password"
                    Click="ChangePassword_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#8B4513" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>


