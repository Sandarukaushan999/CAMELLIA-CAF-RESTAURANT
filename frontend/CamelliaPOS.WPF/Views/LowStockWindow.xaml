<Window x:Class="CamelliaPOS.WPF.Views.LowStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Low Stock Items" Height="450" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  Grid.Row="0"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="80"/>
                <DataGridTextColumn Header="Min Level" Binding="{Binding MinStockLevel}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBox Width="80" Margin="0,0,5,0"
                     ToolTip="Quantity change (+ or -)"
                     Text="{Binding AdjustQuantity, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="200" Margin="0,0,5,0"
                     ToolTip="Reason"
                     Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Apply" Width="100"
                    Command="{Binding ApplyAdjustmentCommand}"/>
        </StackPanel>
    </Grid>
</Window>


